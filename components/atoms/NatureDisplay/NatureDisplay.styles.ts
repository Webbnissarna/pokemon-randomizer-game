import type { ThemeUIStyleObject } from "theme-ui";

export const containerStyle: ThemeUIStyleObject = {
  flexDirection: "column",
  alignItems: "center",
  width: "100%",
};

export const textStyle: ThemeUIStyleObject = {
  fontSize: "sm",
};

export const statContainerStyle: ThemeUIStyleObject = {
  width: "100%",
  justifyContent: "space-around",
  gap: "sm",
};

export const statStyle: ThemeUIStyleObject = {
  borderRadius: "sm",
  paddingX: "md",
  flex: 1,
  textAlign: "center",
};
